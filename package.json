{
  "name": "cookin-up-vue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider &&  vite",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider &&  vite build",
    "preview": "export NODE_OPTIONS=--openssl-legacy-provider &&  vite preview"
  },
  "dependencies": {
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.8"
  }
}
